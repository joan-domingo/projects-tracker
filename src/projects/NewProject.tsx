import React, { FC } from 'react';
import styled from 'styled-components';

const NewProjectContainer = styled.div``;

const NewProject: FC = () => <NewProjectContainer />;

export default NewProject;
